"use strict"; //treat all js code as a newer version

// alert(3 + 3) we are using nodejs not browser
console.log(3
    +3); // Don't write codes like this because code readability should be high
console.log("Prashant");

let name = "Prashant"
let age = 22
let isLoggedIn = false

// number => 2^53 range
// bigInt
// string => "" or ''
// boolean => true/false
// null => standalone value / means empty value / when you ask the type of null it is an object
// undefined => without assigned value
// symbol => unique


// object

console.log(typeof undefined);//unidentified
console.log(typeof null); //object
